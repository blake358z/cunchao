{
  "buildCommand": "npm run build --workspace @cunchao/shared && npm run build --workspace @cunchao/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
